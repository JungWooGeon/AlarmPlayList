<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sample.alarmplaylist">

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlarmPlayList">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".alarm.add_alarm.AddAlarmActivity"
            android:exported="true" />

        <activity
            android:name=".playlist.add_playlist.AddPlaylistActivity"
            android:exported="true" />

        <receiver
            android:name=".alarm.receiver.AlarmReceiver"
            android:exported="false" />

        <service android:name=".alarm.receiver.AlarmService" />

        <activity
            android:name=".playlist.play_youtube.YoutubePlayActivity"
            android:exported="true" />
    </application>

    <queries>
        <intent>
            <action android:name="com.google.android.youtube.api.service.START" />
        </intent>
    </queries>

</manifest>